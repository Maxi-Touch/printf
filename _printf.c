#include "main.h"
#include <unistd.h>
/**
 *_printf - write to stdout formatted text
 * @format: format specifer
 * Return: no of bytes printed
 * On error, -1 is returned, and errno is set appropriately.
 */
int _printf(const char *format, ...);
{
         
	va_list print_list
	va_start(print_list,format)
	va_arg(
    
